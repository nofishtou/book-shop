import { AppSelectElemWithColorDirective } from './app-select-elem-with-color.directive';

describe('AppSelectElemWithColorDirective', () => {
  it('should create an instance', () => {
    const directive = new AppSelectElemWithColorDirective();
    expect(directive).toBeTruthy();
  });
});
