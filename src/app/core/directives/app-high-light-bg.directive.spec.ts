import { AppHighLightBgDirective } from './app-high-light-bg.directive';

describe('AppHighLightBgDirective', () => {
  it('should create an instance', () => {
    const directive = new AppHighLightBgDirective();
    expect(directive).toBeTruthy();
  });
});
